import Button from "@/components/ui/Button";

import { db } from "@/lib/db";

export default async function Home() {

  return (
    <main className="m-2">
      <Button>hey</Button>
    </main>
  );
}
